using Unity.MLAgents;
using UnityEngine;

public class Super_meet_man : Agent
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    2
}
